export const serverPort        = 5000;
export const serverAddress     = `http://localhost:${serverPort}`;
export const notesIdsEndPoint  = `${serverAddress}/notes/ids`
export const notesEndPoint     = `${serverAddress}/notes`
export const searchEndPoint    = `${serverAddress}/search?q=`
